<mvc:View controllerName="learningfiori.invoices.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page id="page" title="{i18n>title}">
   <content>
   <Panel id="panel" headerText="{i18n>selectionScreen}">
   <Label id="labelShipName" labelFor="shipName" text="{i18n>shipName}" class="sapUiSmallMargin"/>
   <Input id="shipName" width="200px" class="sapUiSmallMarginEnd"/>
   <Label id="labelCountry" text="{i18n>country}" class="sapUiSmallMargin"/>
   <Select id="selectCountry" selectedKey="{selectionScreen>/CountryKey}"
                              forceSelection="false" 
                              items="{selectionScreen>/Countries}">
                              <items>
                              <core:Item id="coreItem" key="{selectionScreen>key}"
                              text="{selectionScreen>text}"/>
                              </items>
                              </Select>
   </Panel>
   </content> 
    </Page>
</mvc:View>
